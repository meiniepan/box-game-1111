<!--index.wxml-->
<view class="container" style="padding-top: 180rpx">
    <text class="title"></text>
    <view class="levelBox">
        <view class="box" wx:for="{{levels}}" bindtap="chooseLevel" data-level="{{index}}">
            <button type="primary" >{{item}}</button>
        </view>
    </view>
</view>